/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['img.clerk.com', 'res.cloudinary.com'], // Add 'res.cloudinary.com'
    },
  };
  
  export default nextConfig;
  
